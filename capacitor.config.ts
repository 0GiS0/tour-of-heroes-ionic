import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'tour-of-heroes-ionic',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
